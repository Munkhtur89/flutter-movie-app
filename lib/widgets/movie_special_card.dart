import 'package:flutter/material.dart';

class MoveSpecialCard extends StatelessWidget {
  const MoveSpecialCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
