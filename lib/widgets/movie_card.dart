import 'package:flutter/material.dart';

class MoveCard extends StatelessWidget {
  const MoveCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
